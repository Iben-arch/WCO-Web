# 📘 คู่มือสถาปัตยกรรมและทฤษฎีระบบเบื้องหลัง (System Architecture & AI Theory)
เอกสารชุดนี้จัดทำขึ้นเพื่อใช้อธิบายโครงสร้างของแอปพลิเคชัน WCO-Web การแบ่งหน้าที่ของโฟลเดอร์โค้ด ขั้นตอนการวิเคราะห์ข้อมูล ตลอดจนทฤษฎีคณิตศาสตร์เบื้องหลังการทำงานรันโมเดล AI เพื่อให้คนที่เข้ามาศึกษาต่อสามารถทำความเข้าใจภาพรวมของระบบได้ทันที

---

## 1. 🏗️ สถาปัตยกรรมและโครงสร้างโค้ดส่วนต่างๆ (System Architecture & Code Roles)
แอปพลิเคชันของเราพัฒนาด้วยรูปแบบ **Microservices Architecture** ขนาดย่อม โดยแบ่งหน้าที่ของโค้ดออกเป็น 3 ส่วนหลักที่ทำงานแยกกัน แต่ติดต่อสื่อสารกันผ่าน HTTP API และฐานข้อมูล 

### 1) 🌐 `client-web/` (Frontend - ส่วนหน้าบ้าน)
* **หน้าที่หลัก:** เป็นด่านหน้าคอยรับคำสั่งและรูปภาพจากผู้ใช้งานผ่าน Browser, สร้างปุ่ม, แสดงฟอร์มโต้ตอบ และโชว์ผลลัพธ์การค้นหาสินค้าประมูล
* **ไฟล์โค้ดสำคัญ:** โค้ดในโฟลเดอร์ `src/pages/` และ `src/api/` ทำหน้าที่แปลงข้อมูล Form (รูปภาพการ์ด) และยิง HTTP Request ผ่านไลบรารี Axios ส่งไปยัง Backend
* **ผลลัพธ์ (Output):** หน้า UI ตอบสนองผู้ใช้ (Interactive User Interface) ที่ดึงผลลัพธ์หน้าตาสินค้าการ์ดมาแสดงผลบนจอ

### 2) ⚙️ `server-api/` (Backend Middleware - ส่วนจัดการตรรกะ)
* **หน้าที่หลัก:** เปรียบเสมือนศูนย์กลางจราจร (Hub) ของเว็บไซต์ เขียนด้วยภาษา C# (ASP.NET Core) ชี้เป้าและคัดแยกรูปภาพ ทำงานตั้งแต่รับภาพ, ตรวจสิทธิ์ผู้ใช้งาน (Auth JWT), และคิวรี่ข้อมูลส่งกลับไปกลับมาเซิร์ฟเวอร์
* **ไฟล์โค้ดสำคัญ:**
  * `Controllers/PostsController.cs` (และ Controllers อื่นๆ): ด่านหน้ารับ URL Request จากผู้ใช้งาน
  * `Services/CardDetectionService.cs`: โค้ดสำคัญที่มีฟังก์ชันใช้ไลบรารี **Emgu.CV** ดำเนินการสแกนรูปภาพและใช้คณิตศาสตร์ตัดขอบรูปการ์ดเกมออกมาแบบอัตโนมัติ
  * `Services/SupabaseService.cs`: โค้ดใช้เชื่อมต่อกับฐานข้อมูล Supabase แทรกลงตารางหรือเรียกบันทึกข้อมูลสินค้าไปเก็บบน Cloud DB หรือ Storage
* **ผลลัพธ์ (Output):** ตอบกลับ JSON Response ข้อมูลประมวลผลให้ Frontend ตลอดจนการลบ เซฟ อัปเดตข้อมูลบน Database

### 3) 🤖 `clip-worker/` (AI Image Processor - ส่วนโปรเซสเซอร์วิเคราะห์ภาพ)
* **หน้าที่หลัก:** Service ย่อยเพื่อรัน AI ที่เน้นการกินหน่วยประมวลผลสูง (AI Compute) ถูกปรับให้แยกเขียนด้วยเฟรมเวิร์ก Python สโคปงานมีอย่างเดียวคือรับ "ข้อมูลภาพพิกเซล" และคายออกมาเป็น "เวกเตอร์คณิตศาสตร์" 
* **ไฟล์โค้ดสำคัญ:** `main.py` ทำการดาวน์โหลดพารามิเตอร์รันโมเดล **OpenAI CLIP (ViT-B/32)**
* **ผลลัพธ์ (Output):** ชุดพิกัดตัวเลขทศนิยมเรขาคณิตจํานวน 512 ตัว (512-dimensional vector array) ต่อ 1 รูปภาพ

---

## 2. 🔄 Flow ขั้นตอนการทำงานและผลลัพธ์ (Workflow & Expected Outputs)
เมื่อผู้ใช้งานอัปโหลดรูปภาพใบเดียวเพื่อ **"ตรวจหาสินค้าปลอม"** หรือ **"ค้นหาสินค้าด้วยภาพ"** ระบบจะมีการรับส่งข้อมูลแบบสายพาน (Pipeline Data Flow) ดังนี้:

1. **ส่งภาพเข้าระบบ:** Frontend (`client-web`) ส่งภาพสถานที่จริงเต็มใบ (Raw Image) มาหา `server-api` ผ่าน Endpoint API
2. **ครอปสกัดรูปเฉพาะจุด (Computer Vision):** `server-api` โยนภาพเข้าไฟล์ `CardDetectionService.cs` เพื่อตีเส้นทฤษฎีหาขอบการ์ดและตัดสภาพแวดล้อมฉากหลังรกรุงรังออกม้วนเดียวจบ
   - **(Output):** ภาพการ์ดโฟกัสเน้นๆ ปราศจากพื้นโต๊ะ (Cropped Image Blob)
3. **ตรวจสอบความแท้ ป้องกันรูปเจนเนอเรท (Sightengine):** นำรูปภาพที่ครอปเรียบร้อย ส่งออกไปเช็คกับแพลตฟอร์มนอก Sightengine ก่อน 
   - **(Output):** ค่า % น่าจะเป็น 0-1 ว่าภาพเกิดจาก AI หรือคนถ่าย (ยกตัวอย่างเช่น `0.98`) โดยถ้ารูปหลอกจะยุติโพสหรือบล็อคทันที
4. **ทำเกาเหลาลวดลายภาพ (clip-worker):** ส่งรูปทะลุไปยังฝั่งค่าย Python Worker ให้อ่านองค์ประกอบของศิลปะลายเส้น (Feature Vector)
   - **(Output):** อาร์เรย์ชุดพิกัด 512 ตำแหน่ง ยกตัวอย่างหน้าตาข้อมูลเช่น `[0.081, -0.421, 0.992, ...]` 
5. **หาคู่เหมือนฝาแฝด (pgvector ใน Supabase):** Backend นำเลขอาร์เรย์นี้ลงตาราง Supabase และส่งคำนำสั่งฟังก์ชันให้วัดระยะองศา (Vector Similarity) เพื่อไปหาสินค้ารหัสเดิมๆ ที่มี 512 ตัวเลขนี้ตกค้างคล้ายๆ กันให้มากที่สุด
   - **(Output):** รายการลิสต์แถวสินค้าที่หน้าตาซ้ำกะภาพที่หา คลี่นำมาโชว์ตอบกลับผู้ใช้งาน

---

## 3. 🧠 ทฤษฎีเบื้องหลังโมเดลการคำนวณ (Theoretical Foundations)

### 3.1 OpenAI CLIP (Contrastive Language-Image Pretraining)
CLIP เป็นโมเดล Deep Learning ที่ถูกเทรนให้ "จับคู่" สิ่งที่มีความหมายใกล้เคียงกัน ระหว่างรูปภาพและข้อความ ให้พิกัดตัวเลขไปตกอยู่ในพื้นที่เดียวกันได้ (Joint Embedding Space) อย่างน่าอัศจรรย์

**กระบวนการทำงานภายใน (ViT-B/32):**
- โค้ดของระบบเราอ้างอิงถึงโมเดลลูกย่อยของ CLIP ซึ่งก็คือชั้น **Vision Transformer (ViT)**
- ข้อมูลภาพจะถูกหั่นซอยแบ่งออกเป็นตารางสี่เหลี่ยมเล็กๆ (Patches) ขนาด 32x32 พิกเซล 
- แต่ละ Patch จะประมวลผลผ่าน Matrix Multiplication ของชั้นคณิตศาสตร์ปัญญาประดิษฐ์ (Self-Attention) เพื่อแยกว่าพิกเซลตรงไหนมีความตั้งใจสัมพันธ์กัน (อาทิ ปีกนก มักมีขอบแสงคล้ายหัวนก)
- สุดท้ายโมเดลจะบีบอัดความหมายของตารางทั้งหมดให้เหลือพิกัดเพียงจุดเดียวในแกนนามธรรม **512 มิติ** ($v \in \mathbb{R}^{512}$) 

**สมการความคล้ายคลึงโคไซน์ (Cosine Similarity):**
คณิตศาสตร์ที่ `pgvector` ทำงานในการตัดสินว่าภาพมีส่วนซ้ำคลึงกันคือการวัด "มุม (Angle)" ระหว่างจุดเวกเตอร์ 2 เส้น
$$ \text{Cosine Similarity} (A, B) = \cos(\theta) = \frac{A \cdot B}{\|A\| \|B\|} = \frac{\sum_{i=1}^{n} A_i B_i}{\sqrt{\sum_{i=1}^{n} A_i^2} \sqrt{\sum_{i=1}^{n} B_i^2}} $$
*   $A$ = ชุดเวกเตอร์ข้อมูลภาพฝั่งผู้ใช้อัปโหลด (512 ตัวเลข)
*   $B$ = ชุดเวกเตอร์โพสต์สินค้าที่เก็บบันทึกอยู่เดิมในฐานข้อมูล (512 ตัวเลข)
*   ผลลบสมการจะตกอยู่ในช่วง **-1 ถึง 1** โดยที่หากมุมทั้งสองทับเส้นเบอร์เดียวกัน (มุมห่าง 0 องศา) ผลรวมของสมการคือ **1** แปลว่ารูปการ์ดเหมือนกันระดับเป๊ะ 100%

### 3.2 โมเดลจำแนกภาพ AI-Generated (Sightengine)
Sightengine นำโมเดลตระกูล **CNNs (Convolutional Neural Networks)** หรือสถาปัตยกรรมเช่น ResNet มาวิเคราะห์หารอยต่อลายน้ำ (Artifacts) ที่หลบซ่อนอยู่ในรูปภาพเอไอยอดนิยม

**ทฤษฎีทางสถิติ - Binary Classification:**
- โมเดลถูกเทรนผ่านโครงข่ายประสาทเพื่อคายผลลัพธ์ออกมาเป็นค่าความน่าจะเป็น (Probability) ว่าภาพอยู่ในคลาส 0 (ภาพถ่ายปกติ) หรือคลาส 1 (บอทสร้าง AI)
- พารามิเตอร์สุดท้ายจะพูลลิ่งผ่านฟังก์ชันสถิติ **Sigmoid** 
$$ \sigma(z) = \frac{1}{1 + e^{-z}} $$
- สกอร์ที่ได้จะสะกิดทับถมระหว่าง 0 ไป 1 (`0.98` = น่าจะเป็นภาพ AI 98%) โดยอัลกอริทึมเรียนรู้คำนวณผ่าน **Binary Cross-Entropy Loss** ตลอดการรับฟีดรูปภาพเป็นแสนๆ ใบทำให้โมเดลเกิดความแม่นยำสูงจับโป๊ะภาพบิดเบี้ยวได้

### 3.3 Google Lens (Reverse Image Search / Visual Search)
Google Lens เป็นการขยายขอบเขตของ Vector space ให้เชื่อมเข้าเว็บอินดี้ฐานข้อมูลภาพเว็บอื่นๆ ระดับสเกลโลก
- **Feature Extraction:** ตัว Google Lens จะไม่ได้วิเคราะห์แค่ความคล้ายทางพิกเซลรูปโดยรวม แต่จะเกาะดึงเอา **Keypoints** (รายละเอียดจุดพานอรามา เส้นรอยตัด โค้ง สี) ขึ้นมาเทียบ Signature เฉพาะตัว
- **Approximate Nearest Neighbor (ANN):** เพื่อที่จะเอาภาพขยะไปเทียบกับรูปภาพเวคเตอร์หลายสิบล้านรูปพร้อมๆ กัน ต้องใช้อัลกอริทึมในการทำคลัสเตอร์เวกเตอร์ย่นเวลา **Vector Quantization** เสริมกับการใช้โครงสร้าง Index แบบต้นไม้กราฟ ($O(\log n)$) ช่วยให้การไต่รหัสบาร์เทนเดอร์ภาพมีความเร็วระดับมิลลิวินาที

### 3.4 การตรวจจับและครอปรูปภาพการ์ดอัตโนมัติ (Automated Card Detection & Cropping)
ระบบฝั่งโค้ด `CardDetectionService.cs` มีโครงสร้างปัญญาประดิษฐ์ใช้ไลบรารี **Emgu.CV** เข้ามาช่วยมาร์คจุดและคว้านครอปตัวการ์ดก่อนส่งต่อให้โมเดล AI ตัวอื่น โดยล่าสุดได้มีการอัปเกรดกระบวนการทำงานแบบขั้นบันได (Heuristic Approach) เป็น 5 ขั้นตอนที่ซับซ้อนขึ้น ดังนี้:

1. **Projection-based Grid Detection:** สำหรับภาพถ่ายแผงการ์ดเรียงต่อกันบนโต๊ะ โปรแกรมคำนวณการเปลี่ยนแปลงความลาดชันสีดำขาว **Gradient Magnitude** ผ่านสูตร Sobel Filter ช่องว่างบนโต๊ะความลาดชันจะแตะ 0 (Valley) ส่วนขอบการ์ดมี Gradient สูงกระตุก (Peak) ทำให้ระบบสามารถพลอตเส้นกั้นตาราง Grid X, Y คว้านหาการ์ดได้ทันที
2. **Multi-Scale Contour Detection:** สำหรับภาพการ์ดติดกันยุ่งเหยิง หรือกองกันสะเปะสะปะ
   - ระบบจะทำรูปจำลองเป็น 3 ไซส์ (Multi-scale: สเกล 1x, 0.75x, 0.5x) เพื่อไม่ให้พลาดการ์ดใบเล็กๆ ที่อยู่ไกลลิบ
   - ใช้หลักคณิตศาสตร์ **Adaptive Thresholding** + **Canny Edge Detection** (ส่องหาเส้นกระโดดของฟลักซ์แสง) ควบคู่ไปกับการตรวจจับสัดส่วน Aspect Ratio ของกรอบพิกเซลให้กว้างยาวใกล้เคียงมาตรฐานสุด 2.5:3.5 นิ้ว
3. **Grid Completion:** หากระบบตรวจตารางแล้วพบว่ามี "ฟันหลอ" หรือช่องว่างที่รูปการ์ดหายไปจากแพทเทิร์นตาราง ระบบจะใช้การคำนวณความน่าจะเป็นเพื่อเติมชิ้นส่วนอนุมานตำแหน่ง (Infer) ว่าช่องนั้นควรจะมีการ์ดอยู่
4. **Perspective Correction:** หากผู้ใช้ถ่ายภาพมาเอียง (สมาร์ทโฟนมองลงมาเป็นการ์ดทรงสี่เหลี่ยมคางหมูหรือคางเบี้ยว) โค้ดจะใช้สูตรเรขาคณิต (Perspective Warp/Homography) ดึงมุมทแยงเหล่านั้นให้กลับมาเป็นสี่เหลี่ยมผืนผ้าหน้าตรง 2D (Properly Oriented Rectangular) เพื่อให้ภาพการ์ดแบนราบพร้อมสำหรับการนำไปชุบแป้งทอดในระบบ AI ถัดไป
5. **MSER Text-Region Scoring & Cropping:** เพื่อให้การรับรองผลแม่นยำ 100% ว่ารูปที่ดักมาได้คือการ์ดจริงๆ ไม่ใช่ลวดลายกระเบื้อง ระบบจะวิเคราะห์หาทรงตัวอักษรด้วยจุดบล็อบพิกเซล **MSER (Maximally Stable Extremal Regions)** รูปไหนที่มีกลุ่มก้อนคล้ายอักขระ (ชื่อตัวละคร, Stat พลัง) จะได้คะแนนความหน้าเชื่อถือบวกสูงมาก รูปที่ไม่มีแท็กข้อความจะถูกหักลบ จากนั้นกรอบที่ดีที่สุดจะยืดขยายขอบ Padding เล็กน้อยและคว้านครอปเซฟเป็นรูปย่อยเข้ารหัส Base64 นำไปใช้งานทันที

### 3.5 ค่าพารามิเตอร์คงที่ของโค้ดอัลกอริทึมพิกเซล (Algorithm Configurations)
เพื่อให้การทำงาน Computer Vision ไวและแม่น จึงมีการตั้งค่าหินๆ (Magic Numbers) กะระยะพร้อมเหตุผลรองรับในโค้ดคือ:
*   `CardAspect = 2.5 / 3.5` (อัตราส่วนโกลเด้น ~0.714): อัตราส่วนมาตรฐานที่ผู้จัดพิมพ์แผงการ์ดเกมสะสมใช้ออกแบบพลาสติกซอง (Trading Card Standard Size)
*   `MinAspectRatio = 0.50` ถึง `MaxAspectRatio = 0.92`: ขอบเขตยืดหยุ่นโมเดลเผื่อเวลาแสงหักเห หรือคนถ่ายเอียงกล้อง (Perspective Distortion) ระบบก็จะไม่ปฏิเสธใบหน้าทรงแคบ
*   `MinCardAreaPixels = 5000`: จำนวนพิกเซลขั้นต่ำสุดไว้แยกทรายออกจากกรวดภาพ ป้องกันเอาขยะรบกวนฝุ่น (Noise) หรือปุ่มพลาสติกมารวมนับทำการ์ดเพิ่มให้เปลืองเมม
*   `MaxProcessWidth = 1800`: ทอนขนาดพิกเซลภาพถ่ายล้ำยุค (ระดับหน้าจอ 4K บนกล้องไอโฟน) ให้เหลือไซส์จำเพาะ 1800px ไม่ให้ทำร้ายเครื่อง Server กิน RAM จนสลบ (Performance Optimization) เพิ่มรอบชาร์จข้อมูลได้ไวขึ้นมหาศาลโดยความแม่นเวกเตอร์ไม่ร่วงดรอป
*   `MaxCardsReturned = 24`: แคปลิมิตการครอปได้สูงสุด 24 รูป ป้องกันบัคกระเบื้องกำแพงรอยต่อที่ตีสี่เหลี่มให้ทะลักออกมารบกวนหลักพันตาราง
*   `CropPaddingFraction = 0.012`: ขยายนิ้วตัวครอปขวานฟันอีกระยะ 1.2% กันรอยครอปฉีกกินเนื้อกระดาษแผ่นศิลปะลวดลายแพงๆ หรือ Art ขอบด้านในตกหาย 

---

> [!NOTE]
> ด้วยคู่มือกะทัดรัดฉบับนี้ เราจะได้เห็นว่ากลยุทธ์ฟีเจอร์ AI รูปภาพในโปรเจกต์ กรองตั้งแต่การสกัดตัดเสียงสัญญานกวนทิ้ง (Emgu.CV) คัดกรองภาพปลอม (Sightengine) และหา DNA คล้ายคลึงในอาร์เรย์ตัวเลขเวกเตอร์ 512 มิติ (CLIP) ถือเป็นสถาปัตยกรรมเบื้องหลังชิ้นนวัตกรรมใหม่ ที่ยึดหลักคณิตศาสตร์ด้านเรขาคณิตผสาน Vector Database จนมีประสิทธิภาพฉับไวและแม่นยำสูงเหนือชั้นกว่าเสิร์ชคำปกติหลายระดับ
