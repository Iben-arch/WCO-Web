# Flow การทำงานและทฤษฎีเบื้องหลัง AI Models ในระบบ WCO-Web

เอกสารนี้อธิบายสถาปัตยกรรม ขั้นตอนการทำงาน (Workflow) และทฤษฎีทางคณิตศาสตร์สถิติเบื้องหลังโมเดล AI ที่ใช้ในระบบ ทั้งหน้าบ้านและหลังบ้าน

## 1. Flow การทำงานของระบบ (System Workflow)

เมื่อผู้ใช้อัปโหลดรูปภาพเพื่อ **"ตรวจหาสินค้าปลอม"** หรือ **"ค้นหาสินค้าด้วยภาพ"** ระบบจะมี Flow การวิเคราะห์ข้อมูลรูปภาพดังต่อไปนี้:

1. **Client-Side (Frontend):** ผู้ใช้งานใช้งานผ่านเว็บ React และอัปโหลดไฟล์ภาพ ระบบจะเตรียมไฟล์และส่ง HTTP Request มายัง `server-api` ของ .NET
2. **AI-Generated Image Check (Sightengine API):** 
   - `server-api` ฝั่งแบคเอนด์จะส่งภาพไปยัง **Sightengine API** ก่อนเป็นอับดับแรก เพื่อเช็คว่าเป็นรูปที่ถูกสร้างระบบ Generative AI (เช่น Midjourney, Stable Diffusion, DALL-E) หรือไม่
   - หากความน่าจะเป็น (Probability) สูง ระบบสามารถส่งแจ้งเตือนว่าโพสต์สินค้านั้นเข้าข่าย "ภาพปลอม" หรือปัดตกโพสต์นั้นได้ทันที ป้องกันมิจฉาชีพ
3. **Image Feature Extraction (CLIP Worker):**
   - หากภาพผ่านการคัดกรอง หรือผู้ใช้กำลังใช้ฟีเจอร์ใช้ภาพหาสินค้า `server-api` จะยิง Request ภายในเครือข่ายไปยัง `clip-worker` (Microservice ที่พัฒนาบน Python/FastAPI)
   - โมเดล **OpenAI CLIP (ViT-B/32)** ภายใน `clip-worker` จะแปลงข้อมูลพิกเซลของภาพ ให้กลายเป็น **เวกเตอร์ตัวเลข 512 มิติ (512-dimensional vector)** และส่งอาร์เรย์ตัวเลขนี้กลับมา
   - ตัวเลขเวกเตอร์ชุดนี้จะถูกเซฟเก็บไว้ในตารางฐานข้อมูลที่เปิดส่วนเสริม **`pgvector`** ของ Supabase
4. **Vector Similarity Search (ค้นหาภาพที่คล้ายกัน):**
   - เมื่อต้องการค้นหาภาพที่เหมือนกัน ฐานข้อมูล PostgreSQL (ผ่าน Supabase RPC) จะทำการคำนวณ **Cosine Distance** เปรียบเทียบเวกเตอร์ภาพต้นฉบับ กับเวกเตอร์ของสินค้าทั้งหมดในระบบ
   - ฐานข้อมูลจะส่งคืนโพสต์สินค้าที่มีค่าความคล้ายคลึงใกล้ตัวเลข `1.0` มากที่สุด นำมาเรียงและโชว์ผลลัพธ์
5. **Reverse Image Search (Google Lens Integration):**
   - เพื่อเพิ่มความเชี่ยวชาญในการหาต้นตอภาพ (เช่น ภาพนี้โดนขโมยมาจาก Pinterest หรือไม่) ระบบสามารถพึ่งพากระบวนการ Reverse search ของ **Google Lens API** ดึงข้อมูล Links หน้าเว็บคลังข้อมูลภายนอกมาเทียบเคียงเพิ่มเติม

---

## 2. ทฤษฎีเบื้องหลังโมเดลการคำนวณ (Theoretical & Mathematical Foundations)

### 2.1 OpenAI CLIP (Contrastive Language-Image Pretraining)
CLIP เป็นโมเดล Deep Learning ที่ถูกเทรนให้ "จับคู่" สิ่งที่มีความหมายใกล้เคียงกัน ระหว่างรูปภาพและข้อความ ให้พิกัดตัวเลขไปตกอยู่ในพื้นที่เดียวกัน (Joint Embedding Space)

**กระบวนการทำงานภายใน (ViT-B/32):**
- โค้ดของระบบเราอ้างอิงถึงโมเดลลูกย่อยของ CLIP ซึ่งก็คือชั้น **Vision Transformer (ViT)**
- ภาพจะถูกซอยแบ่งออกเป็นตารางเล็กๆ (Patches) ขนาด 32x32 พิกเซล 
- แต่ละ Patch จะผ่าน Matrix Multiplication ของชั้น Self-Attention ใน Transformer เพื่อแยกว่าพิกเซลตรงไหนมีความสัมพันธ์กัน (เช่น หูแมว มักอยู่ใกล้ตาแมว)
- สุดท้ายโมเดลจะบีบอัดรูปสี่เหลี่ยมทั้งหมดให้เหลือเพียงจุดจุดเดียวในแกน 512 มิติ ($v \in \mathbb{R}^{512}$) 

**สมการความคล้ายคลึงโคไซน์ (Cosine Similarity):**
คณิตศาสตร์ที่ `pgvector` ทำงานในการตัดสินว่าภาพเหมือนกันหรือไม่ คือการวัด "มุม (Angle)" ระหว่างจุดเวกเตอร์ 2 เส้น
$$ \text{Cosine Similarity} (A, B) = \cos(\theta) = \frac{A \cdot B}{\|A\| \|B\|} = \frac{\sum_{i=1}^{n} A_i B_i}{\sqrt{\sum_{i=1}^{n} A_i^2} \sqrt{\sum_{i=1}^{n} B_i^2}} $$
*   $A$ = เวกเตอร์ข้อมูลภาพฝั่งผู้ใช้ (512 แถว)
*   $B$ = เวกเตอร์ข้อมูลสินค้าที่มีอยู่เดิม (512 แถว)
*   ผลลบจะตกในช่วง **-1 ถึง 1** หากเวกเตอร์ขนานชี้ไปทางเดียวกัน (มุม 0 องศา) ผลรวมของสมการคือ **1** = รูปภาพเหมือนร้อยเปอร์เซ็นต์ หรือมีความแคบของมุมมาก

### 2.2 โมเดลจำแนกภาพ AI-Generated (Sightengine)
Sightengine นำโมเดลตระกูล **CNNs (Convolutional Neural Networks)** หรือ ResNet มาเทรนแยกแยะลักษณะเฉพาะกาล (Artifacts/Anomalies) ที่มักมีในภาพ AI สร้าง 

**ทฤษฎีทางสถิติ - Binary Classification:**
- โมเดลถูกเทรนผ่านโครงข่ายประสาทเทียมเพื่อคายผลลัพธ์ออกมาเป็นค่าความน่าจะเป็น (Probability) ว่าภาพอยู่ในคลาส 0 (ภาพคนถ่าย) หรือ 1 (AI)
- คลื่นพารามิเตอร์สุดท้าย จะผ่านฟังก์ชันทางวิชาสถิติที่ชื่อว่า **Sigmoid** 
$$ \sigma(z) = \frac{1}{1 + e^{-z}} $$
- สกอร์ที่ได้จะสะกิดเข้าใกล้ 1.0 (เช่น `0.98` = น่าจะเป็นภาพ AI 98%) ฟังก์ชัน Loss ตอนฝึกสอนจะใช้ **Binary Cross-Entropy Loss** ทำให้โมเดลเกิดความแม่นยำสูงยามเจอเทกเจอร์ประหลาดๆ

### 2.3 Google Lens (Reverse Image Search / Visual Search)
Google Lens เป็นการขยายขอบเขตของ Vector space ให้ไปจับกับฐานข้อมูลสเกลหลักพันล้าน
- **Feature Extraction:** หากเรามองลึก ตัว Google Lens จะไม่ได้วิเคราะห์แค่ความคล้ายทางพิกเซลรวม แต่จะดึง **Keypoints** (รายละเอียดจุดตัด โค้ง สี) ขึ้นมาเรียงเป็น Signature
- **Approximate Nearest Neighbor (ANN):** เพื่อที่จะเอาภาพของเราไปเทียบกับรูปภาพหลายสิบล้านรูปพร้อมกัน Google Lens ต้องใช้อัลกอริทึมในการทำคลัสเตอร์เวกเตอร์ผ่าน **Vector Quantization** หรือโครงสร้าง Index แบบต้นไม้กราฟ ช่วยให้การสแกนหาภาพเหมือนไม่จำเป็นต้องเทียบสมการ Cosine ใหม่ทีละรูป ($O(\log n)$) ช่วยให้ทำเวลาได้เสี้ยววินาที

---

> [!NOTE]
> ระบบค้นหาประมูลสินค้าของเรา นำจุดเด่นเรื่องความเร็วของ Vector DB ใน Supabase ผสานกับโมเดลการเรียนรู้เชิงลึกด้านมุมเรขาคณิต (Cosine) ทำให้หาภาพการ์ดเกมหรือของสะสมที่ถ่ายเบี้ยวๆ หรือมีแสงสะท้อนได้เข้าเป้ากว่าวิธีแบบปกติ (Traditional Keyword Search)
